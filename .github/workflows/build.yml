name: Build and deploy site

on:
  push:
    branches:
      - 'main'
    paths:
      - 'links.json'
      - '.github/workflows/build.yml'
      - 'CNAME'
      - 'img/*'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: davorg/links:links_0.1.3

    steps:

      - name: Checkout
        uses: actions/checkout@v7

      - name: Confirm version of links
        run: linksite -v

      - name: Create page
        run: linksite

      - name: Build pages artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: docs/

  deploy:
    needs: build
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5

